# Prisma / NestJS 用の接続設定
# 値はリポジトリルートの .env と整合させる（docker-compose で起動した PostgreSQL に接続）
DATABASE_URL=postgresql://pick_cash_db_user:pick_cash_db_password@localhost:5432/pick_cash_db?schema=public

# NestJS HTTP サーバーのポート
PORT=3001
